{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run C++ on Terminal",
            "type": "cppdbg",
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${fileDirname}",
            "preLaunchTask": "C/C++: clang++ 활성 파일 빌드",
            "MIMode": "lldb",
            "console": "externalTerminal"
        }
    ]
}
